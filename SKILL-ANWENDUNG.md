# Skill-Anwendung – praxis-zuerichberg-website (21.09.2026, Nachprüfung 23.09.2026)

Alle in `~/.claude/skills/`, im Plugin `design` (synced) und im Marketplace `claude-plugins-official` vorhandenen Skills wurden zu Beginn
gelesen. Leitskill war **design-taste-frontend** (v2). «Gelesen» heisst: SKILL.md vollständig gelesen; «konkrete Anwendung» nennt nur,
was tatsächlich in Dateien umgesetzt wurde. Wo ein Skill nicht oder nur teilweise passte, steht die Begründung – keine pauschalen Haken.

**Stand 21.09. – seit 23.09. überholt, siehe Abschnitt «Nachprüfung»:** `ui-ux-pro-max` (im Auftrag genannt; weder unter `~/.claude/skills`, noch im Plugin-Cache vorhanden –
das Hilfsskript `search.py` aus der Memory-Notiz konnte deshalb nicht laufen). `frontend-design` liegt nur im Marketplace-Cache
(`claude-plugins-official/plugins/frontend-design`), nicht als installiertes Plugin; SKILL.md wurde trotzdem gelesen und angewendet.
**Stitch** (Google) wurde **nicht** benutzt – kein Stitch-Zugang, keine DESIGN.md daraus erzeugt.

| Skill | gelesen | konkrete Anwendung | betroffene Dateien/Komponenten | durchgeführte Prüfung | begründete Einschränkungen |
|---|---|---|---|---|---|
| **design-taste-frontend** (v2, Leitskill) | ja (1206 Z.) | 0.B «Design Read» ausgegeben und im CSS-Kopf festgehalten: Redesign-preserve, trust-first Gesundheitsseite, Zielgruppe inkl. ältere Menschen/Schmerzpatienten; Dials **VARIANCE 4 · MOTION 2 · DENSITY 4** (1.A/1.B Preset «trust/healthcare»); 2.B Aesthetik statt fremdem Designsystem: eigenes System «Horizont» aus Logo (Bergsilhouette, Lime), Aubergine der alten Site; 3.C Icons nur Inline-SVG, 3.D keine Emojis; 4.1 eine Schriftfamilie (Source Sans 3 Variable, lokal), Grundschrift 18 px, Zeilenlänge ≤ 42rem, `text-wrap: balance/pretty`; 4.2 kalibrierte Palette mit Kontrastwerten im Kommentar, Lime nie als Text auf Weiss; 4.3 Layout-Varianz pro Abschnitt (Hero Text-über-Bild, Text+Bild wechselseitig, Akkordeon-Gruppen, Karten, Mosaik-Galerie, Aufruf-Karte); 4.4 eine Rundung (6/12 px), keine Schatten-Stapel; Anti-Default: kein Gradient-Hero, kein «Jane Doe» (nur echte Personen), keine erfundenen Zahlen | `app/globals.css` (Designsystem-Kommentar, Tokens, Utilities), `components/Hero.tsx`, `Horizont.tsx`, `Abschnitt.tsx`, `Behandlungen.tsx`, `Team.tsx`, `Galerie.tsx`, `Bausteine.tsx` | Screenshots 360/390/768/1440 aller 19 Seiten visuell geprüft; Befunde behoben: Team-Karte mit fast quadratischem Foto dehnte den 4:3-Rahmen (`overflow-hidden`), viertes Galeriebild stand als Waise (jetzt breiter Streifen), Titel «Patienteninformationen» lief bei 360 px über (`hyphens: auto` ab 14 Zeichen nur für `h1`) | Skill-Empfehlung «Dependency Verification» für UI-Libraries entfällt: keine UI-Bibliothek eingesetzt (Tailwind + eigene Utilities) |
| **design-taste-frontend-v1** | ja (226 Z.) | Nur als Gegenprobe zu v2: Kapitel 7 «AI Tells» (kein 3-Spalten-Icon-Grid mit Emojis, keine Glassmorphism-Karten, keine Platzhalterdaten) beim Schreiben der Startseite abgehakt; «Standard Hero Paradigm» bewusst nicht als Vollbild-Overlay umgesetzt (Text auf Weiss, Bild darunter – Lesbarkeit für ältere Nutzer) | `app/[sprache]/page.tsx` (über `Seite.tsx`), `components/Hero.tsx` | Sichtprüfung Screenshots `1440-de`, `390-de` | v1 ist laut eigener Beschreibung nur für Projekte gedacht, die v1 exakt brauchen; v2 hatte Vorrang – keine widersprüchlichen v1-Regeln übernommen |
| **frontend-design** (Marketplace) | ja | «Ground in subject matter»: Signatur aus dem tatsächlichen Markenzeichen (Bergsilhouette des Logos) statt generischer Zahnarzt-Metaphern; «plan → review against brief → build → critique»: Brief-Check gegen die 16 Auftragsabschnitte vor dem Bauen, Selbstkritik nach den Screenshots (siehe design-critique) | `components/Horizont.tsx`, `Seitenkopf.tsx` (Lime-Strich), `app/globals.css` | Abgleich Hero-Copy mit Originaltexten (keine erfundenen Claims) | «Restraint»: keine Animationen jenseits Auftauchen/Scroll-Reveal, weil Zielgruppe und Medizinkontext Ruhe verlangen |
| **high-end-visual-design** | ja (98 Z.) | Übernommen: Kap. 2 Anti-Patterns (keine Standard-Schatten, keine reinen Schwarz/Weiss-Kontraste: Tinte `#2a2429`, keine Emoji-Icons); Kap. 4.C Spatial Rhythm: Abschnittsabstand `clamp(4rem…7rem)`, grosszügige Weissräume; Kap. 6 Performance: keine JS-Animationsbibliothek, CSS-only, `prefers-reduced-motion` | `app/globals.css` (`--spacing-abschnitt`, Farben), `components/Abschnitt.tsx` | Reduced-Motion-Test im Prüfbericht («Bewegung reduziert: hero none, scroll none») | **Nicht** übernommen: «Fluid Island»-Navigation, magnetische Buttons, Double-Bezel-Karten, Scroll-Interpolation mit Physik – für eine Praxisseite mit älterer Zielgruppe und MOTION 2 unangemessen; Skill zielt auf Awwwards-Ästhetik, nicht auf Vertrauensseiten |
| **minimalist-ui** | ja (85 Z.) | Kap. 2 Negativ-Constraints (keine Gradients, keine schweren Schatten) und Kap. 3 typografischer Kontrast (Display-Grössen per `clamp`, Etikett-Zeile in Kapitälchen-Stil) angewendet; Kap. 5 Komponenten: flache Karten mit 1-px-Linie | `app/globals.css` (`etikett`, `text-display-*`, `.akkordeon`), `components/Team.tsx` | Sichtprüfung 1440-de_team | Skill verlangt **warmes Monochrom + Pastell** – nicht übernommen, weil die Marke Aubergine/Lime vorgibt (Redesign-preserve) |
| **redesign-existing-projects** | ja (178 Z.) | Design-Audit der alten Site durchgeführt (Einseiter mit JS-Menü, Google Fonts extern, Hintergrund-Slider, Karte ohne Einwilligung, `href=""`-Social-Icons, Team-Widerspruch DE/EN) → Ergebnisse in `docs/INHALTSINVENTUR.md`; «Upgrade ohne Funktionsbruch»: alle Inhalte, PDFs, Anker (jetzt echte Hash-Ziele) und Sprachversionen erhalten; Fix-Priorität: Lesbarkeit/Kontrast → Struktur → Bewegung | `docs/INHALTSINVENTUR.md`, `data/**`, `components/Behandlungen.tsx` | Programmatischer Text-Diff Original ↔ Demo (`docs/MEDIZINISCHE-TEXTE.md`) | Kein CSS der alten Site übernommen (Reset-/Slider-CSS wäre wertlos); Redesign nutzt nur Farben/Logo als Erbe |
| **design:design-system** | ja | Token-Set als `@theme static` (Farben, Schriftgrössen, Abstände, Radien, Easing) mit Rollen-Kommentaren; Komponenten-Utilities (`knopf*`, `etikett`, `feld`, `akkordeon`, `textlink`) statt Ad-hoc-Klassen; Namenskonsistenz Deutsch | `app/globals.css` | Audit-Output des Skills (Naming/Token-Coverage) mündlich abgearbeitet: alle Farben laufen über Tokens (grep `#[0-9a-f]{6}` in `components/` und `app/[sprache]/` → einziger Treffer ist `theme-color` in `layout.tsx`) | Kein separates Design-System-Dokument erzeugt (Umfang der Demo) – Tokens + Kommentare im CSS sind die Quelle |
| **design:ux-copy** | ja | Prinzipien (klar, handlungsorientiert, keine Schuldzuweisung) für alle neu verfassten UI-Texte: CTAs («Anrufen 044 261 33 30», «E-Mail vorbereiten» statt «Absenden»), Fehlermeldung «Bitte geben Sie Ihren Namen an.», Einwilligungstexte ohne Dark Patterns (drei gleichwertige Knöpfe), Karten-Platzhalter erklärt, warum die Karte fehlt; Localization Notes: eigenständige EN-Fassung, Schweizer Schreibweise | `data/de|en/texte.json` (`ui`, `einwilligung`, `formular`), `components/Anfrageformular.tsx`, `Karte.tsx`, `Einwilligung.tsx` | Formular-Test (Pflichtfeld leer → `role=alert`, Fokus auf Feld), Sprachwechsel-Hinweis bei fehlender Übersetzung | Medizinische Texte bewusst **nicht** «verbessert» (Sinn-Erhalt hat Vorrang) |
| **design:accessibility-review** | ja | WCAG-Checkliste angewendet: Kontraste berechnet (Aubergine 9.0:1, Grau 5.9:1, Lime-Text-Variante 4.6:1, Formularrand 3.6:1), Fokus 3 px, Skip-Link, `<html lang>` je Sprache, `hreflang`, native `<details>`, `<dialog>` mit Fokusrückgabe, `aria-invalid`/`role=alert`, Touch-Ziele ≥ 44 px, `prefers-reduced-motion`, Alt-Texte Pflicht (Build bricht ohne ab) | `app/globals.css`, `components/MobilMenue.tsx`, `Einwilligung.tsx`, `Anfrageformular.tsx`, `Sprachwechsel.tsx`, `Fusszeile.tsx`, `lib/content/local.ts` | Puppeteer-Audit (`docs/PRUEFBERICHT.md`): Tab-Reihenfolge, Fokus-Sichtbarkeit, Esc in Dialogen, kleine Ziele (behoben: Sprachwechsel 41 px, Footer-Links, 404-Links; Checkbox jetzt in 44-px-Label), Zoom-200-%-Simulation | Kein echter Screenreader-Test (VoiceOver/NVDA) – nur Emulation; offen in der Übergabe |
| **design:design-critique** | ja | Kritik-Framework (First Impression, Usability, Hierarchie, Konsistenz, A11y) auf die Screenshots angewendet; Befunde → Fixes: Team-Kartenhöhe, Galerie-Waise, Hero-Trennung bei 390 («be-deuten» → Trennung auf `h1` ab 14 Zeichen beschränkt, Dialogtitel nicht mehr getrennt) | `components/Team.tsx`, `Galerie.tsx`, `app/globals.css` | Screenshots vor/nach Fix (`pzb-shots2/1440-de.png`, `390-de.png`) | Kritik von mir selbst, nicht von Dritten; Codex-Review ergänzt die technische Sicht |
| **design:design-handoff** | ja | Handoff-Inhalte (Tokens, Komponenten, Zustände, Responsive-Verhalten, Edge Cases, A11y-Notizen) in `CLAUDE.md` (Designregeln) und `docs/INHALTE-PFLEGEN.md`; Zustände dokumentiert: Einwilligung (keine Entscheidung / nur notwendige / alle / widerrufen), Sprachwechsel ohne Gegenstück, Downloads nur DE | `CLAUDE.md`, `docs/INHALTE-PFLEGEN.md`, `docs/UEBERGABE.md` | – | Kein Figma/Design-Tool vorhanden → Handoff als Markdown + Code |
| **full-output-enforcement** | ja (49 Z.) | Keine Platzhalter/`// …` in Code oder Daten; alle 16 Behandlungen, 3 Teamprofile, 7 Downloads, 4 Rechtstexte, 18 Seiten vollständig ausgegeben; Generatoren erzeugen die JSON komplett | `data/**`, alle Komponenten | `grep -rn "TODO\|…\|Lorem" data components app` → keine Treffer ausser den bewusst dokumentierten «VORBEREITET»-Kommentaren | – |
| **brandkit** | ja (798 Z., Kopf + Logo-/Farb-/Typo-Kapitel) | Nur die Analyse-Teile: Markenessenz aus Logo abgeleitet (Wortmarke, Lime-Akzent, Bergsilhouette → Signaturelement, Schrift Source Sans) | `app/globals.css` (Kommentarkopf), `components/Logo.tsx`, `Horizont.tsx` | – | Skill ist ein **Bildgenerierungs**-Skill (Brand-Boards, Logo-Systeme); Bildgenerierung wurde von Nick ausdrücklich abgelehnt und ein neues Logo war nicht Auftrag → keine Boards erzeugt |
| **imagegen-frontend-web** | ja (987 Z., Kopf + Kap. 1–4) | Nur Prinzipien: Hero-Minimalismus (eine Botschaft, zwei Knöpfe), Section-System mit Varianz, «Second-Read Moment» = Horizontlinie | `components/Hero.tsx` | – | Kernfunktion (Referenzbilder je Sektion generieren) **nicht** ausgeführt – keine Bildgenerierung erlaubt |
| **imagegen-frontend-mobile** | ja (1465 Z., Kopf + Kap. 12–15, 21) | Nur Prinzipien: «First Screen Cleanliness» (mobiler Hero ohne Nebeninhalte), Safe-Area-freundliche Kopfzeile, Navigationsregel (Menü-Dialog mit Telefonknopf), Anti-AI-Tells | `components/Kopfzeile.tsx`, `MobilMenue.tsx`, `Hero.tsx` | Screenshots 360/390 | Skill ist für native App-Screens und Bildgenerierung; nicht anwendbar auf eine Website ohne Bildgenerierung |
| **image-to-code** | ja (1228 Z., Kopf + Kap. 2, 8–10, 14) | Nur Kap. 14 «Hero Minimalism Rules» als Checkliste; Kap. 8 «Clean Analysis» sinngemäss auf die Analyse der **echten** Praxisfotos angewendet (helle Räume, Holz, Weiss → helles Thema) | `components/Hero.tsx`, `app/globals.css` | – | Kernworkflow (erst Designbilder generieren, dann nachbauen) **nicht** ausgeführt – Bildgenerierung abgelehnt; Skill adressiert zudem Codex |
| **gpt-taste** | ja (74 Z.) | Nur Kap. 3 «2-Line Iron Rule» sinngemäss (Hero-Titel max. 3 Zeilen bei 1440, kein 6-Zeilen-Umbruch) und Kap. 7 Content-Bans (keine Platzhalter) | `components/Hero.tsx` | Sichtprüfung 1440-de | **Nicht** übernommen: Python-Zufallslayout, AIDA-Zwang, GSAP-Motion, gapless Bento – widerspricht MOTION 2 und der Medizin-Zielgruppe; GSAP wäre ein externer Request/zusätzliche Abhängigkeit |
| **industrial-brutalist-ui** | ja (92 Z.) | keine | – | – | Ästhetik (Terminal, CRT, militärisch, extreme Typo-Kontraste) passt nicht zu einer Zahnarztpraxis; bewusst nicht angewendet |
| **stitch-design-taste** | ja (184 Z.) | Struktur der DESIGN.md (Atmosphäre, Farbrollen, Typo-Regeln, Hero, Komponenten, Layout, Motion, Anti-Patterns) als Gliederung für den Designsystem-Kommentar und den Abschnitt «Designregeln» in `CLAUDE.md` genutzt | `app/globals.css`, `CLAUDE.md` | – | **Kein Stitch verwendet** (kein Zugang, kein Auftrag); keine DESIGN.md für Stitch erzeugt |
| **find-skills** | ja (141 Z.) | Nur zur Prüfung, ob `ui-ux-pro-max` installierbar wäre: nicht ausgeführt, da keine Installation ohne Nicks Freigabe | – | Verzeichnisse `~/.claude/skills`, Plugin-Cache geprüft | Kein Netzwerkzugriff auf die Skills-CLI ohne Auftrag |
| **pdf** (synced) | ja | `pdftotext`/`pypdf`-Ansatz zur Prüfung der 7 Original-PDFs: Text extrahiert (Inhalt, Stand 2015, Nennung Dr. Deak/Taxpunktwerte), Metadaten gelesen (Titel «Thomas Muster» in 6 Dateien), Zahnunfall.pdf als Fremddokument erkannt | `docs/INHALTSINVENTUR.md`, `data/*/downloads.json` (Hinweise) | Extraktion im Scratchpad (`pzb/pdf/`) | PDFs **nicht** verändert (Metadaten-Bereinigung ist Sache der Praxis) |
| **Skill-Hinweis: Codex-Zusammenarbeit** (aus `~/.claude/CLAUDE.md`, kein Skill) | ja | Runde 1 Architektur (Sprachrouting, Sanity-Modell, Akkordeon vs. Einzelseiten, Export-Fallen, Consent, Datenschutz/Medizin, Top-10-Risiken) → umgesetzt: `[sprache]`-Root-Layout, Document-level Translation mit `uebersetzungsSchluessel`, `dynamicParams=false`, Consent in localStorage ohne preconnect, Formular ohne Gesundheitsdaten; Runde 2 Implementierungs-Review (11 Befunde, 9 umgesetzt: Studio-Root-Layout, Widerruf im Footer, sichtbare Kennzeichnung fehlender Übersetzungen, x-default, JSON-LD ohne unbestätigte Zeiten/`founder`, absolutes Wurzel-Canonical, Hash-`try/catch`, strengerer Export-Prüfer) → `docs/PRUEFBERICHT.md` | `app/[sprache]/layout.tsx`, `lib/content/types.ts`, `lib/einwilligung.ts`, `components/Widerruf.tsx`, `Sprachwechsel.tsx`, `lib/seo.ts`, `scripts/wurzel-schreiben.mjs`, `scripts/export-pruefen.mjs` | Codex-Antworten im Prüfbericht zusammengefasst | Codex-Sandbox kann `next build` nicht ausführen (nur Lesezugriff) |

## Nachprüfung 23.09.2026

Auftrag erneut vollständig gegen die bestehende Demo geprüft (Design «Horizont» bleibt, Entscheid Nick). Die Tabelle oben gilt weiter;
hier nur, was **am 23.09. tatsächlich** gelesen, angewendet oder geprüft wurde. Wo ein Skill heute **nicht** erneut geladen wurde, steht das.

| Skill | gelesen | konkrete Anwendung | betroffene Dateien/Komponenten | durchgeführte Prüfung | begründete Einschränkungen |
|---|---|---|---|---|---|
| **ui-ux-pro-max** (jetzt installiert unter `03_Websiten_hustle/.claude/skills/`) | ja (SKILL.md; Suchskript `search.py` ausgeführt) | Abfragen `"dental clinic healthcare trust" --domain product`, `"focus not obscured" --domain ux`, `"error summary validation" --domain ux`. Umgesetzt: **Focus Not Obscured (WCAG 2.2 AA)** – der fixierte Einwilligungsbanner verdeckte Fokusziele und die Fusszeile → Bannerhöhe als `scroll-padding-bottom` + `body`-Abstand. Geprüft und bestätigt: Fehlermeldung am Feld mit `role=alert` + Fokus (Priorität 8), Touch ≥ 44 px (2), kein horizontales Scrollen (5), SVG-Icons statt Emoji (4) | `components/Einwilligung.tsx`, `app/globals.css` | Tab-Durchlauf 360 px: vorher 17 verdeckte Fokusziele (alte Live-Version), nachher 0; Fusszeile erreichbar | **Nicht übernommen**: Produktempfehlung «Medical Blue #0077B6 + Social-Proof-Landingpage» – die Marke gibt Aubergine/Lime vor (Redesign-preserve) und es gibt keine belegten Bewertungen (keine erfundene Social Proof). «Error Summary» oben im Formular nicht gebaut: nur ein Pflichtfeld, die Inline-Meldung mit Fokus genügt. Kein `--design-system --persist` erzeugt – bestehendes Designsystem, kein Neuentwurf |
| **security-and-hardening** | ja (SKILL.md) | Audit-Triage nach Entscheidungsbaum: 3 «high» (adm-zip, js-yaml, smol-toml) nur über `@sanity/cli` erreichbar, nicht im Pages-Build/Export → «fix soon, kein Blocker», Prüftermin = Sanity-Einrichtung; **kein** `npm audit fix --force` (wäre Major-Downgrade); Patch/Minor-Updates Next 16.3.6, Sanity 6.16; `npm audit signatures`; Secret-Suche im Diff; Security-Header-Vorgabe (CSP) für Vercel dokumentiert; Sanity-Linkvalidierung jetzt auch im Fliesstext (Codex-Befund) | `package.json`, `package-lock.json`, `sanity/schemas/objekte.ts`, `docs/UEBERGABE.md`, `docs/UMSTELLUNG-VERCEL.md` | Signaturen/Attestierungen verifiziert; CI nutzt `npm ci` (Lockfile); `.env*` ignoriert; keine Secrets im Diff | CSP auf GitHub Pages nicht setzbar (keine eigenen Header) – nur dokumentiert |
| **design:accessibility-review** | SKILL.md am 21.09. gelesen, heute nicht erneut geladen | Checkliste erneut angewendet: Fokus nicht verdeckt (s. o.), **Textvergrösserung** über echte Browser-Schriftgrösse (CDP `Page.setFontSizes` 24/32 px) → Überlauf gefunden und behoben (`overflow-wrap: anywhere`, Lebenslauf-Tabelle stapelt unter 22rem); Lebensläufe: Zwischentitel `h4` unter Namen-`h3`, Zeitraum als `th scope=row` (Codex-Befund, WCAG 1.3.1); falscher Alt-Text im Hero korrigiert | `app/globals.css`, `components/RichText.tsx`, `components/Team.tsx`, `data/*/seiten/start.json` | 18 Seiten × 4 Breiten; Schrift 150/200 % bei 390/1440 px und 320 px: 0 Überlauf | kein echter Screenreader, keine echten Geräte |
| **redesign-existing-projects** | am 21.09. gelesen, heute nicht erneut geladen | «Audit first» wiederholt: Originalseite neu abgerufen und Satz für Satz (Trigramm-Abgleich) gegen die Demo geprüft; dabei die vertauschten Hintergrundbilder entdeckt | `docs/INHALTSINVENTUR.md`, `assets/originale/HERKUNFT.md`, `scripts/bilder-optimieren.mjs` | DE 139 / EN 146 Sätze, keine fehlenden Inhalte | – |
| **design:design-critique** | am 21.09. gelesen, heute nicht erneut geladen | Screenshots 390/1440 aller Seiten erneut angesehen; Hero-Bild gegen Alt-Text und Herkunft gegengeprüft (Kontaktbogen aller Originale) | `data/*/seiten/start.json` | Sichtprüfung Team-Lebenslauf und Banner bei 390 px nach den Änderungen | Selbstkritik, keine Drittperson |
| **design:ux-copy** | am 21.09. gelesen, heute nicht erneut geladen | Hero-Unterzeile neutral formuliert (Adresse + wörtliches Spektrum) statt Werbeformel mit «schonend, dauerhaft» | `data/*/seiten/start.json`, `docs/MEDIZINISCHE-TEXTE.md` | Versprechens-Wörter in allen Inhalten gesucht; übrige Treffer sind Originaltext | medizinische Aussagen nicht umformuliert |
| **pdf** | nicht erneut geladen | PDFs heute nur per SHA-1 mit den Originalen verglichen (7/7 identisch) – Inhalt/Metadaten-Befunde vom 21.09. gelten unverändert | `public/downloads/` | Hash-Vergleich | keine erneute Textextraktion nötig, Dateien unverändert |
| **Codex** (kein Skill, `~/.claude/CLAUDE.md`) | – | Runde 3: Implementierungs-Review gegen den Auftrag (0 Blocker, 6 Wichtig, 3 Klein) → 8 umgesetzt, CSP dokumentiert; Runde 4: Review der Korrekturen – Ergebnis in `docs/PRUEFBERICHT.md` | siehe Prüfbericht | siehe Prüfbericht | Codex-Sandbox ohne Netz (Live-Seite nicht erreichbar) |

Nicht erneut angewendet (Ergebnis vom 21.09. unverändert gültig, kein neuer Anlass): design-taste-frontend (v1/v2), frontend-design, high-end-visual-design,
minimalist-ui, gpt-taste, industrial-brutalist-ui, stitch-design-taste, brandkit, image-to-code, imagegen-frontend-web/-mobile (Bildgenerierung weiterhin
nicht freigegeben), design:design-system, design:design-handoff, full-output-enforcement, find-skills (keine Fähigkeitslücke – ui-ux-pro-max ist inzwischen
installiert, nichts nachinstalliert). `browser-testing-with-devtools` setzt einen Chrome-DevTools-MCP-Server voraus, der nicht eingerichtet ist –
Browser-Tests liefen stattdessen mit puppeteer-core.
